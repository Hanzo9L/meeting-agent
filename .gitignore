node_modules
dist-electron
out
release
npm-debug.log*
.env
.conversations/
.knowledge-v2/

# Large eval artifacts - never commit
*.sqlite
*.sqlite-shm
*.sqlite-wal
eval/runs/spikes/
# Indexing job run logs are debug output, not source. Ignore everything in
# this directory so future corpus jobs cannot re-accumulate tracked artifacts.
eval/runs/indexing/*
eval/runs/**/audio/
